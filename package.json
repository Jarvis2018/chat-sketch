{
  "name": "chat-sketch",
  "description": "",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "chat-sketch",
    "manifest": "src/manifest.json",
    "main": "chat-sketch.sketchplugin",
    "assets": [
      "assets/**/*",
      "web-panel/dist/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "scripts": {
    "build": "npm run build:web-panel && skpm-build",
    "build:web-panel": "npm run build --prefix web-panel",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "sketch:link": "skpm-link",
    "dev:web-panel": "npm run dev --prefix web-panel",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@babel/runtime": "^7.28.6",
    "@skpm/builder": "^0.9.5",
    "cross-env": "^7.0.3"
  },
  "author": "Focus <zzdxys@qq.com>",
  "dependencies": {
    "@skpm/fs": "^0.2.6",
    "sketch-json-helper": "^1.1.1",
    "sketch-module-web-view": "^3.5.1"
  }
}
